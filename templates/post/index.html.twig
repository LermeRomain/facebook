{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="container-fluid row">
        <div class="container col-6">
            <h1>New Post</h1>

            {{ form_start(postForm) }}
            {{ form_row(postForm.name) }}
        </div>
        <div class="container">
            <button type="submit" class="btn btn-primary btn-lg" style="margin-top: 20px">Publish</button>
            {{ form_end(postForm) }}
        </div>
    </div>




{% endblock %}
